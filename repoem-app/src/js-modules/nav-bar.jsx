"use strict";
import { useState, useEffect } from 'react';
import { createRoot } from 'react-dom/client';

const NavBar = () => {
    // #region inserting the repoemAPI fetch here too-- will be used for navigating and calling pages
        const [poemsData, setPoemsData] = useState([]);
        const [dataIsLoaded, setDataIsLoaded] = useState(false);
    
        useEffect(() => {
                fetch('http://localhost:8080/data/poems')
                .then((res) => res.json())
                .then((json) => {
                    setPoemsData(json);
                    setDataIsLoaded(true);
                });
        }, []);
    
        if (!dataIsLoaded) {
            return (
                <div>
                    <h1>Please wait...</h1>
                </div>
            );
        }
    // #endregion

    // #region creating button entries for nav bar-- randomized entries
    // #endregion
    
    return (
        // navigation bar-- to be put into each page
        <nav className = "poem-nav">
                <section className = "nav-header">
                    <strong>Navigation</strong>
                </section>
                <div className="not-nav-header">
                    <section className="nav-buttons">
                        <section className = "nav-button">
                            <a href = "./display.html" className="nav-link">poem collection</a>
                        </section>
                        <section className = "nav-button">
                            <a href = "./submit.html" className="nav-link">submit a poem</a>
                        </section>
                    </section>
                    {/* this'll be a list of poems pulled from storage, will be generated by views or by random - this will be something for after initial project completion.*/}
                    {/* <section className = "poem-list">
                        <button className = "poem-entry">example 1</button>
                        <button className = "poem-entry">example 2</button>
                        <button className = "poem-entry">example 3</button>
                        <button className = "poem-entry">example 4</button>
                        <button className = "poem-entry">example 5</button>
                    </section> */}
                    <section className="asset-credits">
                        <section className = "asset-credit">
                            <p>Display Page Definitions are Powered by:</p>
                            <a href = "https://dictionaryapi.dev" className="asset-link">DictionaryAPI by meetDeveloper</a>
                        </section>
                        <section className = "asset-credit">
                            <p>repoemitory Book Icon by:</p>
                            <a href = "https://pixabay.com/illustrations/book-icon-template-stack-of-books-7486253/" className="asset-link">Rezafir7_</a>
                        </section>
                    </section>
                </div>
        </nav>
    );
};

export default NavBar;
